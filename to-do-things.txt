futurebuilder
fromJson()

thêm chức năng: 
--bạn đã mở app bnh lần (tổng lần/ tổng ngày)
--saved bài viết
--edit caption
--chat
--notify khi có ng follow hay like(comment nếu rảnh)
--làm animation cho profile drawer (nếu có thể)
--đổi password
--xem được danh sách follower, following
--chỉ show bài viết của ng nào mình follow                                  check (đọc típs LINE 56)
--navigation drawer		                                                    check
--thay logo svg			                                                    check
--vô profile ngkhac khi bấm vào tên(post card và post detailscreen)			check
--dismiss keyboard sau khi bấm post comment                                 check
--khi xóa post (nếu xóa khi xem trong trang profile hay explore)            check
thì sau khi xóa cần pop hẳn về màn trước để refresh lại màn 
(cập nhật lại)
ý tưởng: thêm biến atExplore?, nếu true thì không cho phép xóa,
chỉ cho phép user xóa khi ở trang profile hoặc home

lưu lại email để khi đăng nhập kh cần nhập lại
(secure storage)
tham khảo trên chrome (widget chung)

scaffold có 2 body, tắt đc 1 body


